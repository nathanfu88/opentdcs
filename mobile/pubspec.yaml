name: opentdcs_mobile
description: "Minimal tDCS control application for ESP32 via Bluetooth"
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  # BLE Communication
  flutter_blue_plus: ^2.0.0

  # State Management
  provider: ^6.0.5
  shared_preferences: ^2.5.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
